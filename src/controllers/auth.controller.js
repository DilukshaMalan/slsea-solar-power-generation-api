const asyncHandler = require('../utils/asyncHandler');

// TODO (Phase 6): implement login -> verify password_hash -> sign JWT
// with { type: 'user', user_id, role, province_id, district_id, scope }
// exports.login = asyncHandler(async (req, res) => { ... });
